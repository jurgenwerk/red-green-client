{{nav-bar
  period=period
  currentUser=session.currentUser
  goPeriodBack=(action 'goPeriodBack')
  goPeriodNext=(action 'goPeriodNext')
  logout=(action 'logout')
}}

{{side-bar
  sumDifference=sumDifference
  expenseSum=expenseSum
  incomeSum=incomeSum
  currencySymbol=session.currentUser.currencySymbol
}}

<div class="main">
  {{outlet}}
</div>

{{global-links}}
